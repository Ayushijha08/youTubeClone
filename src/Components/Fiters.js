import React from 'react'
import './filters.css'
const Filters = ({text}) => {
  return (
    <div>
        
<button className="filter-options active">{text}</button>
          </div>
    
  )
}

export default Filters